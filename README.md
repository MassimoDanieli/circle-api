# Circle API (FastAPI)

Simple API that calculates area and circumference of a circle from a radius.

## Run locally

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
uvicorn app.main:app --reload

